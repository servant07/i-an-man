using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TutorialCanvas : MonoBehaviour
{
    public LineRenderer lr_rightCanvas, lr_leftCanvas;

    public GameObject rightController, leftController;

    void Start()
    {
        
    }

    void Update()
    {
        /*
        lr_rightCanvas.SetPosition(0);
        lr_rightCanvas.SetPosition();

        lr_leftCanvas.SetPosition();
        lr_leftCanvas.SetPosition(); 
        */
    }
}
